services:
  - type: web
    name: flask-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app
    runtime: python
    region: oregon
    pythonVersion: 3.12.3
